#include "stats.h"

Stats Statistics::ComputeStatistics(const std::vector<float>& ) {
    //Implement statistics here
    float dummy x = 0;
    Stats var ;
    var.average = x;
    var.max = x;
    var.min = x;
    return var;
}
